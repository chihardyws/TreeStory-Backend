{% extends 'base.html.twig' %}

{% block body %}
    <h1>Forgot Password</h1>

    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.email) }}

        <div>
            <input type="submit" value="Request a password" />
        </div>
    {{ form_end(form) }}
{% endblock %}
